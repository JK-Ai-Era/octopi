/**
 * Channel Adapter 类型
 *
 * @canonical src/integration/types/channels.ts
 * 此处为向后兼容 re-export。
 */
export type { ChannelMessage, ChannelReply, ChannelAdapter } from '../../integration/types/channels.js';
