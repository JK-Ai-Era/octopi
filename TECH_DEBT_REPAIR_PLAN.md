# Octopi 技术债务修复方案

## 1. 修复目标

### 总体目标
- 安全性提升：消除工具参数校验缺失带来的安全风险
- 功能完善：补齐 ProviderPool 探活机制等缺失功能
- 架构一致性：解决 KnowledgeStage 与新架构的兼容问题
- 代码质量：提升可维护性、可测试性和代码整洁度

### 具体指标
- 安全漏洞：减少 2 个高风险安全漏洞
- 功能缺失：补齐 3 个关键功能缺失
- 代码质量：消除 4 个架构不一致问题
- 测试覆盖：提升整体测试覆盖率到 80%+

---

## 2. 修复任务分解

### 阶段一：安全与功能修复（第1-2周）

#### 2.1 工具参数校验完善（高优先级）
问题：src/harness/plugin-ecosystem/tools/registry.ts 只检查必填参数，缺少类型、枚举、范围校验

修复方案：
1. 扩展参数校验函数
   - 添加类型校验（string, number, boolean, object, array）
   - 添加枚举值校验
   - 添加数值范围校验（minimum, maximum）
   - 添加字符串长度校验（minLength, maxLength）
   - 添加正则表达式校验（pattern）

2. 添加校验规则配置
   - 支持 JSON Schema 格式的参数定义
   - 支持自定义校验函数
   - 支持条件校验（if-then）

3. 改进错误处理
   - 提供详细的错误信息
   - 支持多语言错误消息
   - 添加校验错误码

验收标准：
- 所有内置工具参数都能正确校验
- 类型错误在调用前被捕获
- 枚举值错误在调用前被拒绝
- 数值范围错误在调用前被检测
- 单元测试覆盖率达到 90%+

风险评估：
- 风险：可能影响现有工具的兼容性
- 缓解：添加向后兼容模式，逐步迁移

---

#### 2.2 ProviderPool 探活机制（高优先级）
问题：src/harness/concurrency/provider-pool.ts 缺少定期探活不健康 slot 的机制

修复方案：
1. 实现健康检查定时器
   - 添加可配置的检查间隔（默认 30 秒）
   - 支持按 slot 配置不同的检查策略
   - 添加检查超时设置

2. 实现健康检查逻辑
   - 发送轻量级测试请求
   - 检查响应时间和成功率
   - 支持自定义健康检查端点

3. 实现自动恢复机制
   - 健康检查通过后自动标记为健康
   - 支持渐进式恢复（从低权重开始）
   - 添加恢复后的观察期

4. 添加监控和告警
   - 记录健康检查日志
   - 发送健康状态变化事件
   - 支持配置告警阈值

验收标准：
- 不健康的 slot 能自动恢复
- 健康检查不影响正常请求性能
- 支持配置检查间隔和策略
- 有完整的监控和日志记录

风险评估：
- 风险：健康检查可能增加系统负载
- 缓解：使用轻量级检查，支持配置检查频率

---

#### 2.3 KnowledgeStage 架构适配（高优先级）
问题：src/harness/task-system/knowledge/stage.ts 依赖已删除的 ContextPipeline Stage 接口

修复方案：
1. 迁移到 ContextEngine 体系
   - 将 KnowledgeStage 重构为 ContextEngine 实现
   - 适配新的 AssembleParams/AssembleResult 接口
   - 保持原有功能不变

2. 实现新的知识注入机制
   - 支持按需检索知识
   - 支持知识优先级排序
   - 支持知识压缩和摘要

3. 更新配置和文档
   - 更新配置格式
   - 更新使用文档
   - 提供迁移指南

4. 添加兼容性层
   - 保持旧接口的向后兼容
   - 添加废弃警告
   - 提供自动迁移工具

验收标准：
- KnowledgeStage 适配新的 ContextEngine 体系
- 原有功能完全保持
- 配置格式向后兼容
- 有完整的迁移文档

风险评估：
- 风险：可能影响现有知识注入功能
- 缓解：保留旧实现作为备选方案

---

### 阶段二：架构优化（第3-4周）

#### 2.4 CLI 文件拆分（中优先级）
问题：src/cli.ts 文件过大（1084行），职责过多

修复方案：
1. 按功能模块拆分
   - src/cli/index.ts - 主入口，命令解析
   - src/cli/daemon.ts - 守护进程管理
   - src/cli/web-ui.ts - Web UI 管理
   - src/cli/commands/ - 各个命令实现
   - src/cli/utils/ - 公共工具函数

2. 提取公共模块
   - 配置加载逻辑
   - 进程管理逻辑
   - 日志记录逻辑
   - 错误处理逻辑

3. 改进命令解析
   - 使用更现代的命令行解析库
   - 支持命令自动补全
   - 支持配置文件覆盖命令行参数

4. 添加单元测试
   - 为每个模块编写独立测试
   - 模拟命令行参数
   - 测试错误处理

验收标准：
- CLI 文件拆分为多个小文件
- 每个模块职责单一
- 单元测试覆盖率达到 85%+
- 命令行功能完全保持

风险评估：
- 风险：可能影响现有命令行工具的使用
- 缓解：保持接口完全兼容

---

#### 2.5 Web Runtime Store 清理（中优先级）
问题：src/integration/web/runtime/store.ts 存在遗留代码和向后兼容问题

修复方案：
1. 统一数据结构
   - 将 MessageRecord 迁移到 ConversationItem
   - 添加数据转换工具
   - 保持向后兼容

2. 清理遗留代码
   - 移除 deprecated 标记
   - 统一使用新数据结构
   - 添加数据迁移工具

3. 优化性能
   - 减少数据转换开销
   - 优化缓存策略
   - 添加内存使用监控

4. 改进文档
   - 更新 API 文档
   - 添加使用示例
   - 提供迁移指南

验收标准：
- 统一使用 ConversationItem 数据结构
- 移除所有 deprecated 标记
- 性能无明显下降
- 有完整的迁移文档

风险评估：
- 风险：可能影响现有 Web UI 的兼容性
- 缓解：保留兼容性层，逐步迁移

---

### 阶段三：质量提升（第5-6周）

#### 2.6 测试覆盖完善（中优先级）
问题：测试覆盖率工具缺失，部分模块测试不足

修复方案：
1. 配置测试覆盖率工具
   - 添加 @vitest/coverage-v8 依赖
   - 配置覆盖率报告
   - 设置覆盖率阈值

2. 补充缺失测试
   - 识别测试薄弱的模块
   - 编写缺失的单元测试
   - 添加集成测试

3. 改进测试质量
   - 添加边界条件测试
   - 添加错误处理测试
   - 添加性能测试

4. 自动化测试流程
   - 集成到 CI/CD 流程
   - 添加测试覆盖率报告
   - 设置质量门禁

验收标准：
- 整体测试覆盖率达到 80%+
- 核心模块测试覆盖率达到 90%+
- 有完整的测试报告
- 集成到 CI/CD 流程

风险评估：
- 风险：可能增加开发时间
- 缓解：优先覆盖核心模块

---

#### 2.7 代码清理和优化（低优先级）
问题：存在 deprecated 代码、重复代码、性能瓶颈

修复方案：
1. 清理 deprecated 代码
   - 移除向后兼容的重新导出
   - 更新导入路径
   - 添加迁移脚本

2. 消除代码重复
   - 提取公共工具函数
   - 统一相似逻辑
   - 优化代码结构

3. 性能优化
   - 识别性能瓶颈
   - 优化关键路径
   - 添加性能监控

4. 代码风格统一
   - 配置 ESLint
   - 统一代码风格
   - 添加代码格式化

验收标准：
- 移除所有 deprecated 标记
- 消除明显代码重复
- 关键路径性能提升 20%+
- 代码风格完全统一

风险评估：
- 风险：可能引入新 bug
- 缓解：充分测试，逐步重构


## 3. 验收标准

### 功能验收
- 所有现有功能完全保持
- 新增功能按设计工作
- 性能无明显下降
- 无新增安全漏洞

### 质量验收
- 测试覆盖率达到 80%+
- 代码风格统一
- 文档完整更新
- 无 deprecated 标记

### 架构验收
- 模块职责单一
- 依赖关系清晰
- 接口设计合理
- 扩展性良好

### 安全验收
- 工具参数校验完善
- 无注入漏洞
- 无权限绕过
- 有完整的安全测试

---

